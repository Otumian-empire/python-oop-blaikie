# class = blue print for an instance
# instance = a contructed object of a class
# type = indicates the class the instance belongs to
# attribute = any object value: object.attribute
# method = a "callable attribute" defined in a class

# var = 'hello world'
# print(type(var))
# print(var.upper())

# a car can be seen as a class object
# the car class provides the blue print for a car object
# each instacce of the car does the same thing (methods)
# however each car in has its own state (attributes)
